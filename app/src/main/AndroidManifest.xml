<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="shopping.microkart">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="shopping.microkart.MainActivity"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="shopping.microkart.UserActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="shopping.microkart.BargainCartActivity"
            android:label="@string/title_activity_hawk"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="shopping.microkart.CartActivity"
            android:label="@string/action_cart" />
        <activity
            android:name="shopping.microkart.LoginActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="shopping.microkart.DescriptionActivity" />
        <activity android:name="shopping.microkart.ChatActivity" />
        <activity android:name="shopping.microkart.SellerDetailsActivity" />
        />
        <activity
            android:name="shopping.microkart.SellerActivity"
            android:label="@string/title_activity_seller"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
